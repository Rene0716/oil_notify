name: Run Python Script

# on:
#   schedule:
#     - cron: '45 2 * * 6'  # 每週日的中午（UTC時間）
on:
  push:
    branches:
      - 'main'

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Run Python Script
        run: oil_notify.py  # 執行您的 Python 腳本
